sudo: false
language: node_js 

node_js:
  - node 

install:
  - npm ci

script:
  # - npm run test

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: gh-pages 